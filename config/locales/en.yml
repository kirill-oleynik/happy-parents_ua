---
en:
  activerecord:
    errors:
      models:
        product:
          attributes:
            slug:
              bad_slug: Should contain only downcase letters, numbers & dashes
  active_admin:
    dashboard: Dashboard
    dashboard_welcome:
      call_to_action: Custom call to action
      welcome: Custom welcome message
